{{- if .Values.global.tls.enabled }}
# API ingress (with rewrite-target for path stripping)
{{- if .Values.studioApi.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "linto-studio.fullname" . }}-api
  labels:
    {{- include "linto-studio.labels" . | nindent 4 }}
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    {{- if and (eq .Values.global.tls.mode "acme") .Values.global.tls.createCertificate }}
    {{- $tlsSecret := lookup "v1" "Secret" .Release.Namespace (include "linto-studio.tlsSecretName" .) }}
    {{- if not $tlsSecret }}
    cert-manager.io/cluster-issuer: letsencrypt-prod
    {{- end }}
    {{- end }}
    {{- if .Values.studioApi.ingress.stripPrefix }}
    nginx.ingress.kubernetes.io/rewrite-target: /$2
    nginx.ingress.kubernetes.io/use-regex: "true"
    {{- end }}
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - {{ .Values.global.domain }}
      secretName: {{ include "linto-studio.tlsSecretName" . }}
  rules:
    - host: {{ .Values.global.domain }}
      http:
        paths:
          {{- if .Values.studioApi.ingress.stripPrefix }}
          - path: {{ .Values.studioApi.ingress.path }}(/|$)(.*)
            pathType: ImplementationSpecific
          {{- else }}
          - path: {{ .Values.studioApi.ingress.path }}
            pathType: Prefix
          {{- end }}
            backend:
              service:
                name: {{ include "linto-studio.fullname" . }}-api
                port:
                  number: {{ .Values.studioApi.service.port }}
{{- end }}
---
# WebSocket ingress
{{- if .Values.studioWebsocket.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "linto-studio.fullname" . }}-websocket
  labels:
    {{- include "linto-studio.labels" . | nindent 4 }}
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-http-version: "1.1"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "3600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "3600"
    {{- if .Values.studioWebsocket.ingress.stripPrefix }}
    nginx.ingress.kubernetes.io/rewrite-target: /$2
    nginx.ingress.kubernetes.io/use-regex: "true"
    {{- end }}
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - {{ .Values.global.domain }}
      secretName: {{ include "linto-studio.tlsSecretName" . }}
  rules:
    - host: {{ .Values.global.domain }}
      http:
        paths:
          - path: /socket.io/
            pathType: Prefix
            backend:
              service:
                name: {{ include "linto-studio.fullname" . }}-websocket
                port:
                  number: {{ .Values.studioWebsocket.service.port }}
          {{- if .Values.studioWebsocket.ingress.stripPrefix }}
          - path: {{ .Values.studioWebsocket.ingress.path }}(/|$)(.*)
            pathType: ImplementationSpecific
          {{- else }}
          - path: {{ .Values.studioWebsocket.ingress.path }}
            pathType: Prefix
          {{- end }}
            backend:
              service:
                name: {{ include "linto-studio.fullname" . }}-websocket
                port:
                  number: {{ .Values.studioWebsocket.service.port }}
{{- end }}
---
# Frontend ingress (no rewrite-target — serves static assets as-is)
{{- if .Values.studioFrontend.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "linto-studio.fullname" . }}-frontend
  labels:
    {{- include "linto-studio.labels" . | nindent 4 }}
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - {{ .Values.global.domain }}
      secretName: {{ include "linto-studio.tlsSecretName" . }}
  rules:
    - host: {{ .Values.global.domain }}
      http:
        paths:
          - path: {{ .Values.studioFrontend.ingress.path }}
            pathType: Prefix
            backend:
              service:
                name: {{ include "linto-studio.fullname" . }}-frontend
                port:
                  number: {{ .Values.studioFrontend.service.port }}
{{- end }}
{{- else }}
# TLS is disabled - using HTTP ingress
# API ingress
{{- if .Values.studioApi.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "linto-studio.fullname" . }}-api
  labels:
    {{- include "linto-studio.labels" . | nindent 4 }}
  annotations:
    kubernetes.io/ingress.class: nginx
    {{- if .Values.studioApi.ingress.stripPrefix }}
    nginx.ingress.kubernetes.io/rewrite-target: /$2
    nginx.ingress.kubernetes.io/use-regex: "true"
    {{- end }}
spec:
  ingressClassName: nginx
  rules:
    - host: {{ .Values.global.domain }}
      http:
        paths:
          {{- if .Values.studioApi.ingress.stripPrefix }}
          - path: {{ .Values.studioApi.ingress.path }}(/|$)(.*)
            pathType: ImplementationSpecific
          {{- else }}
          - path: {{ .Values.studioApi.ingress.path }}
            pathType: Prefix
          {{- end }}
            backend:
              service:
                name: {{ include "linto-studio.fullname" . }}-api
                port:
                  number: {{ .Values.studioApi.service.port }}
{{- end }}
---
# WebSocket ingress
{{- if .Values.studioWebsocket.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "linto-studio.fullname" . }}-websocket
  labels:
    {{- include "linto-studio.labels" . | nindent 4 }}
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/proxy-http-version: "1.1"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "3600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "3600"
    {{- if .Values.studioWebsocket.ingress.stripPrefix }}
    nginx.ingress.kubernetes.io/rewrite-target: /$2
    nginx.ingress.kubernetes.io/use-regex: "true"
    {{- end }}
spec:
  ingressClassName: nginx
  rules:
    - host: {{ .Values.global.domain }}
      http:
        paths:
          - path: /socket.io/
            pathType: Prefix
            backend:
              service:
                name: {{ include "linto-studio.fullname" . }}-websocket
                port:
                  number: {{ .Values.studioWebsocket.service.port }}
          {{- if .Values.studioWebsocket.ingress.stripPrefix }}
          - path: {{ .Values.studioWebsocket.ingress.path }}(/|$)(.*)
            pathType: ImplementationSpecific
          {{- else }}
          - path: {{ .Values.studioWebsocket.ingress.path }}
            pathType: Prefix
          {{- end }}
            backend:
              service:
                name: {{ include "linto-studio.fullname" . }}-websocket
                port:
                  number: {{ .Values.studioWebsocket.service.port }}
{{- end }}
---
# Frontend ingress (no rewrite — serves static assets as-is)
{{- if .Values.studioFrontend.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "linto-studio.fullname" . }}-frontend
  labels:
    {{- include "linto-studio.labels" . | nindent 4 }}
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  ingressClassName: nginx
  rules:
    - host: {{ .Values.global.domain }}
      http:
        paths:
          - path: {{ .Values.studioFrontend.ingress.path }}
            pathType: Prefix
            backend:
              service:
                name: {{ include "linto-studio.fullname" . }}-frontend
                port:
                  number: {{ .Values.studioFrontend.service.port }}
{{- end }}
{{- end }}
