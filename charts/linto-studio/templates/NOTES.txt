LinTO {{ .Chart.Name }} has been installed.

{{- if .Values.global.tls.enabled }}
Access at: https://{{ .Values.global.domain }}/cm
{{- else }}
Access at: http://{{ .Values.global.domain }}/cm
{{- end }}

To check status:
  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}

Components deployed:
  - Studio API
  - Studio Frontend
  - Studio WebSocket
  - MongoDB
