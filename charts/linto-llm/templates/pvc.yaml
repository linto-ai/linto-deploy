{{- if and .Values.postgres.enabled .Values.postgres.persistence.enabled (not .Values.global.storage.database.hostPath) }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "linto-llm.fullname" . }}-postgres-data
  labels:
    {{- include "linto-llm.labels" . | nindent 4 }}
    app.kubernetes.io/component: postgres
spec:
  accessModes:
    - ReadWriteOnce
  {{- if or .Values.postgres.persistence.storageClass .Values.global.storageClass }}
  storageClassName: {{ .Values.postgres.persistence.storageClass | default .Values.global.storageClass }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.postgres.persistence.size }}
{{- end }}
---
{{- if and .Values.redis.enabled .Values.redis.persistence.enabled (not .Values.global.storage.database.hostPath) }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "linto-llm.fullname" . }}-redis-data
  labels:
    {{- include "linto-llm.labels" . | nindent 4 }}
    app.kubernetes.io/component: redis
spec:
  accessModes:
    - ReadWriteOnce
  {{- if or .Values.redis.persistence.storageClass .Values.global.storageClass }}
  storageClassName: {{ .Values.redis.persistence.storageClass | default .Values.global.storageClass }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.redis.persistence.size }}
{{- end }}
